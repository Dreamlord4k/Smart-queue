# tests/AGENTS.md

- Обязательные unit-тесты для каждого этапа перечислены в PLAN.md в разделе «Проверка» — не считать этап готовым без них.
- Тесты на бизнес-правила (кто может двигать запись, отсутствие автоотмены при пустой активной очереди, сохранение отказа с `absence_reason`, независимость очередей и формула EMA) приоритетнее тестов на UI-детали. RSVP-логику и тесты «нулевых подтверждений» не создавать и удалить при обнаружении.
- Не писать интеграционные тесты на некритичные ветки (например, на каждый текст уведомления) — только на критический путь.
