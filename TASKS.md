# TASKS.md

## T-001 — Скелет проекта и модель User/Group
Статус: DONE Владелец: backend
Этап PLAN.md: 1. Аккаунты, роли, группы
Разрешённые файлы: `backend/models/user.py`, `backend/models/group.py`, `backend/db/migrations/*`, `docker-compose.yml`

### Детали реализации
- создать модели `User` (role, full_name, password_hash, group_id?, telegram_id?) и `Group` (name);
- поднять Postgres в docker-compose, применить первую миграцию;
- заготовка `.env.example` со всеми переменными из PLAN.md.

### Проверки: 2/2
- [x] миграция применяется на чистой базе без ошибок;
- [x] `docker-compose up` поднимает postgres и backend без падений.

Текущая попытка: `docs/agent/tasks/T-001/20260906T093011Z_backend_a01.md`
Блокер: нет.
Следующий шаг: задача завершена и принята; перейти к T-002.

---

## T-002 — Регистрация/логин, JWT access+refresh
Статус: DONE Владелец: backend
Этап PLAN.md: 1. Аккаунты, роли, группы
Разрешённые файлы: `backend/auth/*`, `backend/routes/auth.py`, `tests/auth/*`

### Детали реализации
- `POST /auth/register`, `POST /auth/login`, `POST /auth/refresh`;
- хэш пароля (bcrypt/argon2), роль в payload access-токена;
- `DELETE /auth/me` — удаление аккаунта и связанных записей (см. PRODUCT.md — обязательное требование).

### Проверки: 3/3
- [x] логин выдаёт корректную пару access+refresh;
- [x] refresh обновляет access без повторного пароля;
- [x] роль в токене реально ограничивает доступ к teacher-only эндпоинтам (403 для студента).

Текущая попытка: `docs/agent/tasks/T-002/20260906T095956Z_backend_a01.md`
Блокер: нет.
Следующий шаг: задача завершена и принята; перейти к T-003.

---

## T-003 — Создание сессии и автоматическая алфавитная очередь
Статус: DONE Владелец: backend
Этап PLAN.md: 2. Сессии приёма, автоматическая очередь и дашборды
Разрешённые файлы: `backend/models/session.py`, `backend/models/queue_entry.py`, `backend/routes/sessions.py`, `backend/db/migrations/*`, `tests/sessions/*`

### Детали реализации
- создать `Session` с `frozen=false` и без RSVP-полей;
- при `POST /sessions` в одной транзакции создать по одному `QueueEntry` на каждого уникального студента из групп и явного списка;
- отсортировать исходные записи по `full_name`, при совпадении — по `id`; назначить последовательные позиции;
- не создавать никакой автоматической отмены сессии: `cancelled` доступен только через действие преподавателя.

### Проверки: 4/4
- [x] создание сессии сразу создаёт записи всех уникальных выбранных студентов в алфавитном порядке;
- [x] повторное попадание студента через группу и явный список не создаёт дубликат;
- [x] пустая активная очередь не меняет статус сессии на `cancelled`;
- [x] в модели, API и тестах отсутствуют RSVP-поля и сценарии «нулевых подтверждений».

Текущая попытка: `docs/agent/tasks/T-003/20260906T103111Z_backend_a01.md`
Блокер: нет.
Следующий шаг: задача реализована, проверки пройдены, PR #3 принят и смёржен человеком; перейти к T-004.

---

## T-004 — Отказ от конкретной сессии и «Мои очереди»
Статус: DONE Владелец: fullstack
Этап PLAN.md: 2–3. Автоматическая очередь, EMA/ETA
Разрешённые файлы: `backend/models/queue_entry.py`, `backend/routes/queue.py`, `backend/queue/eta.py`, `frontend/src/screens/MyQueues.tsx`, `tests/queue/*`

### Детали реализации
- добавить `status=absent` и nullable `absence_reason`; не смешивать с `lock_reason`;
- реализовать `POST /students/me/queues/{eid}/absence` только для собственной записи и только в данной сессии;
- сохранять отказ в БД, исключать его из активной очереди, нормализовать позиции и пересчитывать ETA;
- сделать главным экраном студента «Мои очереди» с карточками активных/предстоящих сессий и предупреждением о потенциальном пересечении без автоматических действий.

### Проверки: 5/5
- [x] отказ сохраняет запись и необязательный `absence_reason`, а не удаляет её;
- [x] отказ исключает студента из активной очереди, нормализует позиции и пересчитывает ETA остальных;
- [x] причина отказа доступна преподавателю только соответствующей сессии;
- [x] отказ в одной сессии не изменяет другую очередь того же студента;
- [x] «Мои очереди» одновременно показывает несколько сессий одного дня с независимыми полями и статусами.

Текущая попытка: `docs/agent/tasks/T-004/20260906T104641Z_fullstack_a01.md`
Блокер: нет.
Следующий шаг: задача реализована, проверки пройдены, PR #4 принят и смёржен человеком; выполнить FIX-001 перед следующей задачей.

---

## T-005 — Завершение приёма, EMA и автоматический вызов
Статус: TODO Владелец: backend
Этап PLAN.md: 3. Динамическая очередь, EMA и ETA
Разрешённые файлы: `backend/models/service_stat.py`, `backend/models/queue_entry.py`, `backend/routes/queue.py`, `backend/queue/eta.py`, `backend/config.py`, `backend/db/migrations/*`, `tests/queue/*`

### Детали реализации
- добавить teacher-only `POST /sessions/{id}/queue/{eid}/done` и `POST /sessions/{id}/queue/{eid}/skip` только для текущей `called`-записи одного из каналов своей сессии;
- при `done` сохранять фактическую длительность обслуживания и обновлять `ServiceStat.ema_estimate`, `variance` и `n_observations`; `alpha=0.3` вынести в конфиг, первое наблюдение становится начальным средним;
- при `done`/`skip` освобождать канал, автоматически переводить следующую `waiting`-запись в `called`, нормализовать позиции и пересчитывать ETA;
- если уже реализованный отказ вызывается для `called`-записи, освобождать её канал и поднимать следующего так же, как при `skip`;
- считать ETA жадным распределением по `capacity` каналам с учётом текущих вызванных студентов и актуального среднего времени; пустая очередь не отменяет сессию автоматически;
- сериализовать конкурирующие завершения транзакцией и блокировкой строк либо эквивалентным безопасным для выбранной БД способом.

### Проверки: 0/7
- [ ] первое завершение и последующие завершения обновляют EMA, дисперсию и число наблюдений по формуле с `alpha=0.3`;
- [ ] завершать/пропускать запись может только преподаватель-владелец сессии и только в её текущем канале;
- [ ] `done` фиксирует фактическое время, а `skip` не добавляет его в статистику;
- [ ] каждый освободившийся канал автоматически вызывает следующую ожидающую запись;
- [ ] отказ уже вызванного студента освобождает только его канал и вызывает следующего;
- [ ] ETA корректно пересчитывается для `capacity=1` и `capacity>1`;
- [ ] конкурентные запросы не вызывают двойной переход, дубликаты позиций или автоматическую отмену сессии.

Текущая попытка: —
Блокер: зависит от T-004.
Следующий шаг: реализовать серверный жизненный цикл текущего приёма и покрыть его интеграционными тестами.

---

## T-006 — Заморозка списка
Статус: TODO Владелец: backend
Этап PLAN.md: 4. Self-service реордер, фиксация и заморозка списка
Разрешённые файлы: `backend/models/session.py`, `backend/routes/sessions.py`, `tests/sessions/*`

### Детали реализации
- добавить teacher-only `POST /sessions/{id}/freeze`, который устанавливает `session.frozen=true` только для сессии преподавателя-владельца;
- сделать операцию повторяемой без побочных эффектов и возвращать актуальное состояние сессии;
- сохранить серверный признак заморозки как обязательный запрет для любых последующих перестановок; применение запрета в reorder-эндпоинте входит в T-007.

### Проверки: 0/4
- [ ] преподаватель-владелец замораживает свою сессию;
- [ ] студент и другой преподаватель получают отказ и не меняют `frozen`;
- [ ] повторный запрос к уже замороженной сессии безопасен и оставляет `frozen=true`;
- [ ] ответ API и последующее чтение сессии показывают одно и то же состояние заморозки.

Текущая попытка: —
Блокер: зависит от T-003.
Следующий шаг: реализовать отдельное серверное действие заморозки и его проверки доступа.

---

## T-007 — Self-reorder и фиксация места
Статус: TODO Владелец: backend
Этап PLAN.md: 4. Self-service реордер, фиксация и заморозка списка
Разрешённые файлы: `backend/models/queue_entry.py`, `backend/models/queue_move_log.py`, `backend/routes/queue.py`, `backend/db/migrations/*`, `tests/queue/*`

### Детали реализации
- добавить `PATCH /sessions/{id}/queue/reorder` для перемещения только собственной `waiting`-записи студента через `{entry_id, target_entry_id, placement: before|after}`;
- на бэкенде запрещать движение чужой записи, движение собственной записи с `locked=true`, перестановку после `session.frozen=true` и вставку на позицию, занятую зафиксированной записью;
- после успешного движения атомарно нормализовать позиции, пересчитать ETA и записать в `QueueMoveLog` автора, запись, старую/новую позицию и время;
- добавить изменение `locked` и свободного nullable `lock_reason` для собственной ожидающей записи; снятие флага снова разрешает владельцу двигать запись;
- не смешивать `lock_reason` с `absence_reason` и не вводить классификацию причин.

### Проверки: 0/6
- [ ] студент не может двигать чужую запись, даже если подставил её идентификатор (403);
- [ ] нельзя вставить запись на место, занятое записью с `locked=true`;
- [ ] владелец не двигает свою зафиксированную запись до снятия `locked`;
- [ ] после `frozen=true` порядок не меняет никто;
- [ ] успешная перестановка атомарно обновляет позиции и создаёт точную запись `QueueMoveLog`;
- [ ] `lock_reason` и `absence_reason` сохраняются и выдаются как независимые поля.

Текущая попытка: —
Блокер: зависит от T-006.
Следующий шаг: реализовать серверные правила перестановки и аудит движений.

---

## T-008 — Управление сессией и дашборд преподавателя: backend
Статус: TODO Владелец: backend
Этап PLAN.md: 2. Сессии приёма, автоматическая очередь и дашборды
Разрешённые файлы: `backend/models/session.py`, `backend/models/group.py`, `backend/models/queue_entry.py`, `backend/routes/sessions.py`, `backend/routes/groups.py`, `backend/db/migrations/*`, `tests/sessions/*`, `tests/groups/*`

### Детали реализации
- добавить `GET /sessions/mine` со списком предстоящих, активных и завершённых сессий текущего преподавателя;
- добавить teacher-only `PATCH /sessions/{id}` для изменения длительности и допустимых переходов статуса, включая запуск, паузу, закрытие и явную отмену; чужая сессия недоступна;
- запускать сессию можно только после заморозки списка; закрытие возвращает данные для отчёта: принято, пропущено, средняя длительность, плановое и фактическое время и среднюю ошибку ETA, восстановленную из временных меток завершённых записей;
- добавить `POST /sessions/{id}/participants` и `DELETE /sessions/{id}/participants/{eid}` для явного управления участниками уже созданной, в том числе идущей, сессии;
- нового участника добавлять без дубликата в конец активной очереди с последующим пересчётом позиций и ETA; удаление не должно стирать уже завершённую историю;
- добавить `GET /groups`: минимальный список групп для выбора при регистрации и состав групп для teacher-only форм создания сессии и поиска студента.

### Проверки: 0/7
- [ ] `/sessions/mine` не раскрывает сессии другого преподавателя и корректно разделяет их по состояниям;
- [ ] только преподаватель-владелец меняет статус, длительность и состав своей сессии;
- [ ] невалидный переход статуса отклоняется, а запуск незамороженной сессии запрещён;
- [ ] участник, добавленный после создания или запуска сессии, получает одну запись в конце активной очереди и корректный ETA;
- [ ] повторное добавление не создаёт дубликат;
- [ ] удаление участника нормализует активную очередь, но не уничтожает завершённую историю;
- [ ] `/groups` возвращает только необходимые для соответствующего контекста поля и даёт форме сессии состав выбранных групп.

Текущая попытка: —
Блокер: зависит от T-003.
Следующий шаг: реализовать API дашборда и управление жизненным циклом и участниками сессии.

---

## T-009 — Дашборд преподавателя и управление очередью: frontend
Статус: TODO Владелец: frontend
Этап PLAN.md: 2–4. Дашборд преподавателя и управление динамической очередью
Разрешённые файлы: `frontend/src/screens/TeacherDashboard.tsx`, `frontend/src/screens/TeacherSession.tsx`, `frontend/src/components/teacher/*`, `frontend/src/api/*`, `frontend/src/types/*`, `frontend/src/App.tsx`, `frontend/src/**/*.test.*`

### Детали реализации
- показать преподавателю собственные предстоящие, активные и завершённые сессии;
- реализовать форму создания сессии через выбор групп и явных студентов без RSVP;
- на экране текущей сессии показать каналы, очередь, ETA, фиксацию и обе независимые причины, а также действия `done`, `skip`, `freeze`, `start`, `pause`, `close`;
- добавить формы изменения длительности и добавления/удаления участника уже созданной или идущей сессии;
- после закрытия показывать число принятых и пропущенных, среднюю длительность, плановое и фактическое время и среднюю точность ETA; drag-and-drop преподавателя не входит в эту задачу и может подключаться после готовности T-007 и студенческого экрана T-010.

### Проверки: 0/5
- [ ] дашборд показывает только сессии текущего преподавателя и их актуальные состояния;
- [ ] форма создаёт сессию из групп и явных студентов, не создавая RSVP-сценарий;
- [ ] кнопки управления вызывают соответствующие API и обновляют экран без ручной перезагрузки;
- [ ] участника можно добавить на лету и удалить с немедленным обновлением очереди и ETA;
- [ ] закрытая сессия показывает число принятых/пропущенных, среднюю длительность, плановые и фактические показатели и среднюю точность ETA.

Текущая попытка: —
Блокер: зависит от T-005, T-006 и T-008.
Следующий шаг: собрать учительские экраны поверх готового API управления сессией и очередью.

---

## T-010 — Недостающие экраны студента
Статус: TODO Владелец: frontend
Этап PLAN.md: 1–4. Аутентификация и управление своей очередью
Разрешённые файлы: `frontend/src/screens/Login.tsx`, `frontend/src/screens/Register.tsx`, `frontend/src/screens/Queue.tsx`, `frontend/src/screens/Settings.tsx`, `frontend/src/components/student/*`, `frontend/src/auth/*`, `frontend/src/api/*`, `frontend/src/types/*`, `frontend/src/App.tsx`, `frontend/src/**/*.test.*`

### Детали реализации
- сделать экраны регистрации и входа, хранение и обновление пары access+refresh и восстановление пользовательской сессии после перезапуска клиента;
- после входа направлять пользователя на экран своей роли; регистрация студента включает выбор группы, а одинаковые имена не используются как уникальный идентификатор;
- на экране конкретной очереди показывать позицию, статус, ETA и соседей и разрешать drag-and-drop только собственной `waiting`-записи;
- добавить для собственной записи фиксацию/снятие фиксации со свободным `lock_reason` и отказ от конкретной сессии с отдельным `absence_reason`;
- в настройках добавить удаление аккаунта с явным подтверждением; данные других участников нельзя редактировать из UI.

### Проверки: 0/6
- [ ] регистрация и вход обрабатывают ошибки и направляют пользователя по роли;
- [ ] access обновляется через refresh, а вход сохраняется после перезапуска клиента;
- [ ] совпадающие имена студентов не смешивают аккаунты, очередь или разрешения;
- [ ] drag-and-drop отправляет изменение только для собственной ожидающей записи и откатывает UI после отказа сервера;
- [ ] фиксация, `lock_reason`, отказ и `absence_reason` отображаются и изменяются независимо;
- [ ] удаление профиля требует подтверждения и вызывает `DELETE /auth/me`.

Текущая попытка: —
Блокер: зависит от T-007.
Следующий шаг: реализовать маршрутизацию, аутентификационные формы и полный self-service экран студента.

---

## T-011 — Реалтайм очереди через Redis и WebSocket
Статус: TODO Владелец: fullstack
Этап PLAN.md: 6. Реалтайм
Разрешённые файлы: `backend/realtime/*`, `backend/routes/ws.py`, `backend/routes/queue.py`, `backend/routes/sessions.py`, `backend/main.py`, `backend/config.py`, `backend/requirements.txt`, `frontend/src/hooks/useQueueSocket.ts`, `frontend/src/realtime/*`, `frontend/src/api/*`, `frontend/src/screens/Queue.tsx`, `frontend/src/screens/TeacherSession.tsx`, `docker-compose.yml`, `tests/realtime/*`

### Детали реализации
- публиковать изменения сессии и очереди в Redis Pub/Sub-канал `session:{id}` после успешного коммита транзакции;
- добавить WebSocket-подписку с проверкой JWT и доступа пользователя к конкретной сессии;
- обновлять студенческий и преподавательский экраны по событиям без полной перезагрузки;
- реализовать документированный fallback на опрос API раз в 5 секунд при недоступном WebSocket; для первого среза допускается оставить только этот fallback, если решение зафиксировано в файле попытки.

### Проверки: 0/4
- [ ] изменения `done`, `skip`, состава, перестановки, фиксации, заморозки и статуса публикуют событие только после сохранения в БД;
- [ ] посторонний пользователь не может подписаться на чужую сессию;
- [ ] два открытых клиента получают новое состояние очереди без ручной перезагрузки;
- [ ] при потере WebSocket клиент переходит на 5-секундный polling и возвращается к подписке без дублирования обновлений.

Текущая попытка: —
Блокер: зависит от T-009 и T-010.
Следующий шаг: реализовать канал событий и проверить синхронизацию в двух окнах. Кандидат первого среза — оставить только 5-секундный polling с записью решения в отчёте.

---

## T-012 — Telegram-линковка и уведомления
Статус: TODO Владелец: backend
Этап PLAN.md: 5. Telegram-уведомления
Разрешённые файлы: `backend/models/notification_log.py`, `backend/models/telegram_link_code.py`, `backend/routes/telegram.py`, `backend/notifications/*`, `backend/db/migrations/*`, `backend/config.py`, `backend/requirements.txt`, `bot/*`, `frontend/src/screens/Settings.tsx`, `frontend/src/api/*`, `docker-compose.yml`, `tests/notifications/*`

### Детали реализации
- связать существующий аккаунт с `telegram_id` через одноразовый короткоживущий код; Telegram остаётся только каналом уведомлений и не становится способом входа;
- принимать код привязки через `POST /telegram/webhook` в production-режиме и тем же обработчиком сообщений в режиме long polling;
- добавить экран выпуска кода и отображения состояния линковки в настройках;
- после `done`/`skip` отправлять три типа уведомлений: мягкое по таймеру, попадание в заданный буфер перед вызовом и финальное при вызове;
- вести `NotificationLog` с уникальным ключом события и получателя, чтобы повторный расчёт или доставка не создавали дубликаты;
- мягкие уведомления рассчитывать фоновой задачей, а буферные и финальные — синхронно от изменения очереди через слой событий;
- для локальной разработки запускать бота в long polling; webhook/HTTPS подключается инфраструктурой T-014. Если к защите нет домена/HTTPS, long polling официально допустим, но решение фиксируется в файле попытки.

### Проверки: 0/6
- [ ] одноразовый код нельзя применить повторно или после истечения срока;
- [ ] Telegram связывается только с уже аутентифицированным аккаунтом и не выдаёт токены входа;
- [ ] после последовательности `done`/`skip` корректно срабатывают мягкое, буферное и финальное уведомления;
- [ ] повторная обработка одного события не создаёт дубликат в `NotificationLog` и не отправляет сообщение повторно;
- [ ] ошибка Telegram не откатывает изменение очереди и остаётся доступной для повторной доставки;
- [ ] интеграционные тесты не обращаются к реальному Telegram API и используют подмену транспорта.

Текущая попытка: —
Блокер: зависит от T-005.
Следующий шаг: реализовать безопасную линковку, дедупликацию и три порога уведомлений.

---

## T-013 — Демо-генератор и сквозной сценарий
Статус: TODO Владелец: fullstack
Этап PLAN.md: 7. Синтетический генератор данных и демо-сценарий
Разрешённые файлы: `scripts/generate_demo_data.py`, `scripts/run_demo_scenario.py`, `tests/demo/*`, `README.md`

### Детали реализации
- генерировать группы, преподавателя и студентов, минимум две сессии одного дня с общим студентом и автоматически созданными очередями;
- воспроизводить через публичный API, а не прямые изменения БД или UI, отказ с причиной, self-reorder, фиксацию, заморозку, запуск и серию `done`/`skip` со случайными задержками вокруг заданной длительности;
- включить `capacity=1` и `capacity>1`, изменение EMA/ETA, realtime-обновления и Telegram-события с тестовым транспортом;
- не использовать RSVP и не демонстрировать автоматическую отмену пустой очереди;
- описать воспроизводимый запуск и ожидаемые контрольные точки ручного показа.

### Проверки: 0/5
- [ ] повторный запуск создаёт изолированный и предсказуемый набор демо-данных;
- [ ] две сессии одного дня содержат общего студента, но независимые записи и статусы;
- [ ] сценарий проходит отказ, перестановку, фиксацию, заморозку и динамический приём только через API;
- [ ] после `done`/`skip` корректно меняются EMA, ETA, каналы и события для `capacity=1` и `capacity>1`;
- [ ] сквозной прогон подтверждает realtime и Telegram без реальной отправки внешнему получателю.

Текущая попытка: —
Блокер: зависит от T-005, T-007, T-008, T-011 и T-012.
Следующий шаг: собрать воспроизводимый API-сценарий и выполнить ручной демо-прогон из PLAN.md.

---

## T-014 — Redis, bot, nginx/HTTPS и CI
Статус: TODO Владелец: devops
Этап PLAN.md: 8. Инфраструктура
Разрешённые файлы: `docker-compose.yml`, `backend/Dockerfile`, `backend/main.py`, `frontend/Dockerfile`, `bot/Dockerfile`, `nginx/*`, `scripts/deploy/*`, `.github/workflows/*`, `.env.example`, `README.md`, `tests/infra/*`

### Детали реализации
- довести `docker-compose.yml` до полного локального контура: backend, frontend, postgres, redis и bot с healthcheck и явными зависимостями;
- добавить и подключить `/health`-эндпоинт для проверки готовности backend;
- настроить nginx как единую точку входа для HTTP и WebSocket и подготовить выпуск/обновление сертификата Let's Encrypt;
- описать безопасную конфигурацию окружения без секретов в репозитории и режим Telegram long polling для локальной разработки;
- добавить минимальный CI, который устанавливает зависимости и запускает backend- и frontend-проверки;
- работу можно готовить параллельно, но финальную интеграцию выполнять после T-011 и T-012; при сокращении объёма сначала исключать CI, а nginx/HTTPS/Redis — только после согласования с пользователем.

### Проверки: 0/5
- [ ] `docker compose up` поднимает полный контур, а healthcheck подтверждает готовность backend, postgres и redis;
- [ ] через nginx работают API, frontend и WebSocket upgrade;
- [ ] конфигурация сертификата проходит проверку, а инструкция описывает первичный выпуск и автоматическое обновление;
- [ ] bot запускается локально в long polling без публикации токена и использует общий backend/redis-контур;
- [ ] минимальный CI запускает те же обязательные backend- и frontend-проверки, что локальная инструкция.

Текущая попытка: —
Блокер: финальная интеграция зависит от T-011 и T-012; подготовку можно вести параллельно.
Следующий шаг: собрать полный compose-контур и проверить единый вход через nginx. Кандидат второго среза — сначала исключить CI; nginx/HTTPS/Redis сокращать только после согласования.
