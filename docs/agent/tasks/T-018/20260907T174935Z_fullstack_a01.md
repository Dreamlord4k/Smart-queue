## Задача: T-018 — тексты прогноза, таймер преподавателя и разморозка
Исполнитель: fullstack
Начато: 20260907T174935Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Заменить видимую аббревиатуру ETA на понятное «примерно», добавить живой таймер called-записей на экране преподавателя и teacher-only идемпотентную разморозку с realtime-событием и повторно разрешённым reorder.

Ветка: `task/T-018`. Пункты 4–6 объединяются в один коммит и один PR по прямому указанию пользователя; исходные отдельные ветки `task/TIMER-teacher` и `task/UNFREEZE` не создаются. `TASKS.md` не изменяется.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `backend/AGENTS.md`, `tests/AGENTS.md`

### Изменённые файлы
- `backend/routes/queue.py` — `called_at` текущего участника доступен экрану преподавателя.
- `backend/routes/sessions.py` — teacher-only идемпотентный `POST /sessions/{id}/unfreeze` и событие `session.unfrozen`.
- `frontend/src/api/teacher.ts`, `frontend/src/types/teacher.ts` — контракт разморозки и `called_at`.
- `frontend/src/screens/MyQueues.tsx`, `Queue.tsx`, `TeacherSession.tsx` — видимое «Примерно» вместо ETA; таймер и управление разморозкой.
- `frontend/src/components/teacher/Teacher.css` — градиент таймера, красное состояние перерасхода.
- тесты рядом с изменёнными backend/frontend-компонентами.
- этот файл попытки.

### Команды и результаты
- Frontend targeted: 20 passed; production build — passed.
- Frontend full: 15 файлов, 70 тестов — passed.
- Backend targeted без SQLite-неспецифичного concurrency-кейса: 14 passed.
- Backend full `tests/queue tests/sessions` на SQLite: 45 passed, 2 failed. Оба падения известны и не относятся к T-018: `SELECT FOR UPDATE` не сериализует конкурентный `done` в SQLite; драйвер SQLite/Python 3.14 некорректно читает искусственный UUID из теста. Финальная проверка — CI на PostgreSQL.
- `python -m compileall -q backend tests` — passed.

### Блокеры / найденные риски
- FIX-007 уже смёржен в `main`; T-018 создаётся поверх merge-коммита `2774d2a` без повторения его diff.
- Локальный PostgreSQL отсутствует, поэтому конкурентная блокировка подтверждается обязательным GitHub Actions-прогоном.

### Точный следующий шаг
Создать один коммит в `task/T-018`, показать compare-diff, открыть один PR без merge и дождаться CI.

### SUBAGENT_HANDOFF
Субагенты не используются.
