## Задача: T-014 (скелет CI)
Исполнитель: devops
Начато: 20260906T155403Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Только скелет CI из T-014: один новый файл `.github/workflows/ci.yml` (триггер push/PR, установка зависимостей, backend pytest, frontend npm test + build; версии экшенов фиксированные, секретов нет). Плюс этот отчёт `docs/agent/tasks/T-014/<UTC>_devops_a01.md`. Не тронуты: `backend/*`, `frontend/src`, `tests/*`, `TASKS.md`, `PRODUCT.md`, `AGENTS.md`, compose. Ветка: `task/T-014-ci`.

### Уточнения пользователя
- Postgres в CI: с postgres и env (сервис postgres:16-alpine, тестовые DB_URL/JWT_SECRET, шаг `alembic upgrade head`).
- Ветки триггера: только main.
- Пути тестов: относительные `tests/...` (в CI checkout кладёт репозиторий в корень, абсолютных `/tests` там нет; `/tests/...` — путь монтирования внутри docker-контейнера backend).

### Просмотренные файлы
- TASKS.md (T-014, только чтение)
- backend/Dockerfile (python 3.12) и backend/requirements.txt
- frontend/Dockerfile (node 22) и frontend/package.json (scripts: test = vitest run, build = tsc -b && vite build)
- .env.example (имена переменных DB_URL/JWT_SECRET)

### Изменённые файлы
- `.github/workflows/ci.yml` (новый: jobs backend + frontend, триггеры push/PR на main)
- `docs/agent/tasks/T-014/20260906T155403Z_devops_a01.md` (этот файл)

### Содержимое ci.yml (кратко)
- backend: ubuntu-latest + сервис postgres:16-alpine (healthcheck pg_isready), env DB_URL/JWT_SECRET — тестовые значения только для CI; checkout@v4.2.2, setup-python@v5.3.0 (3.12), pip install, alembic upgrade head, `pytest -q -p no:cacheprovider tests/auth tests/sessions tests/queue`.
- frontend: checkout@v4.2.2, setup-node@v4.1.0 (node 22, npm-кэш), `npm ci`, `npm test -- --run`, `npm run build` в `frontend/`.

### Команды и результаты
- YAML-валидация через одноразовый контейнер python:3.12-slim + pyyaml → OK (ключи name/on/jobs; jobs backend+frontend; шагов 5+5).
- Полный прогон CI локально не выполнялся (скелет; раннер GitHub). Локальные эквиваленты команд уже покрыты отчётами T-005 (pytest 24 passed, npm test/build) и T-006 (sessions 10/10, auth+sessions 15/15); сами команды в ci.yml повторяют их дословно с поправкой путей.

### Блокеры / найденные риски
- БЛОКЕР: GitHub-интеграция недоступна в этом окружении (нет инструментов коммита/PR через интеграцию). Прямые git-команды в обход запрещены поручением, поэтому коммит в ветку `task/T-014-ci` и PR в main НЕ выполнены. Требуется действие человека: закоммитить `.github/workflows/ci.yml` и этот файл, открыть PR без merge.
- Риск: скелет не покрывает остальное T-014 (redis, bot, nginx/HTTPS, полный compose-контур) — это вне scope данной задачи.

### Точный следующий шаг
- Человеку: закоммитить `.github/workflows/ci.yml` + этот отчёт в ветку `task/T-014-ci`, открыть PR в main без merge; проверить первый прогон Actions после пуша.

### SUBAGENT_HANDOFF
Субагенты не используются.
