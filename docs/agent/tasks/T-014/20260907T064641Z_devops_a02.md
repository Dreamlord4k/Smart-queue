## Задача: T-014
Исполнитель: devops
Начато: 20260907T064641Z
Предыдущая попытка: `docs/agent/tasks/T-014/20260906T155403Z_devops_a01.md`

### Цель и разрешённый scope
Завершить частично реализованный инфраструктурный этап: полный compose-контур с readiness и зависимостями, единая nginx-точка входа для HTTP/WebSocket, подготовка Let's Encrypt, безопасная конфигурация long polling и полный минимальный CI. Разрешены только `docker-compose.yml`, `backend/Dockerfile`, `backend/main.py`, `frontend/Dockerfile`, `bot/Dockerfile`, `nginx/*`, `scripts/deploy/*`, `.github/workflows/*`, `.env.example`, `README.md`, `tests/infra/*` и этот файл попытки.

### Просмотренные файлы
- `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- предыдущая попытка T-014 и шаблон отчёта
- `backend/AGENTS.md`, `tests/AGENTS.md`
- текущие compose, Dockerfile, `/health`, bot worker, realtime, CI, env и README

### Изменённые файлы
- `backend/main.py` — readiness Postgres/Redis с HTTP 503 при недоступности;
- `docker-compose.yml` — полный health/dependency graph и nginx/certbot;
- `nginx/default.conf.template`, `nginx/https.conf.template` — HTTP/TLS, API, SPA, WebSocket;
- `scripts/deploy/docker-compose.https.yml`, `issue_certificate.sh`, `renew_certificate.sh`;
- `.env.example`, `README.md` — безопасные placeholder-переменные, long polling и HTTPS-инструкция;
- `.gitignore` — по прямому указанию пользователя исключён локальный `.env`;
- `.github/workflows/ci.yml` — полный backend/frontend-набор и compose smoke-test;
- `tests/infra/test_infrastructure.py` — три целевых инфраструктурных теста;
- этот файл попытки.

### Команды и результаты
- GitHub: PR #23 (T-013) подтверждённо смёржен в `main` (`7c1a464d`).
- Целевой прогон `pytest -q -p no:cacheprovider tests/infra` → `3 passed, 2 warnings in 0.52s`.
- Python compile, shell syntax и YAML parse для compose/overlay/CI → успешно.
- Frontend `npm test -- --run` → `50 passed`; `npm run build` → успешно.
- Полный backend-прогон во временной SQLite → `50 passed, 6 failed`: оставшиеся тесты требуют PostgreSQL UUID/конкурентных блокировок и закономерно несовместимы с SQLite; регрессий T-014 в выводе нет.
- GitHub Actions run #33 на PostgreSQL → backend-тесты, frontend-тесты/build, compose config и полный smoke-test health/API/frontend/WebSocket завершены успешно.

### Блокеры / найденные риски
- Блокеров нет: отсутствующие на локальном хосте Docker/PostgreSQL проверки подтверждены зелёным GitHub Actions run #33.
- Локальный HTTPS включается после первичного выпуска сертификата; обычный `docker compose up` использует HTTP и long polling.

### Точный следующий шаг
Дождаться повторного зелёного Actions-прогона после защитной строки `.env`, затем передать PR человеку на проверку и merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
