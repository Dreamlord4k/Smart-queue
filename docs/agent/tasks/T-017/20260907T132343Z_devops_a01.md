## Задача: T-017 — единый скрипт первичного развёртывания
Исполнитель: devops
Начато: 20260907T132343Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Добавить интерактивные `scripts/deploy/setup.sh` и `scripts/deploy/setup.ps1` с пятью сценариями первичного запуска, production HTTPS, безопасного обновления, досева групп и диагностики; общие шаги оформить функциями. Добавить короткую секцию в `README.md`. Код сервисов и `TASKS.md` не менять.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- существующие compose/nginx/certbot/Telegram deploy-файлы
- T-013 генератор и его детерминированная логика двух групп

### Изменённые файлы
- `scripts/deploy/setup.sh` — интерактивный мастер для Linux/macOS, безопасный сценарий по Enter, общие функции шагов и пять сценариев.
- `scripts/deploy/setup.ps1` — эквивалентный мастер для Windows/PowerShell.
- `README.md` — короткая инструкция запуска и неразрушающие гарантии.
- этот файл попытки.

### Команды и результаты
- `bash -n scripts/deploy/setup.sh` — успешно.
- Изолированный smoke с подменёнными Docker/curl/git: сценарии 1–5 — успешно; сценарий 1 повторно — успешно, хэш существующего `.env` не изменился.
- Отдельный smoke `--scenario 3 --https` — успешно: plain-старт, сертификат/оверлей, HTTPS health и сводка.
- Production на исходных плейсхолдерах — ожидаемый отказ с отдельной инструкцией для `POSTGRES_PASSWORD`, `DB_URL`, `JWT_SECRET`, `DOMAIN`, `LETSENCRYPT_EMAIL`.
- Enter без выбора на реальной машине выбрал только сценарий 5, `.env` не создал и завершился точным сообщением об отсутствующем Docker CLI.
- Поиск разрушающих команд `down -v`, `down --volumes`, `rm -rf`, рекурсивного `Remove-Item` — совпадений нет.
- README без добавленной секции имеет Git blob `df6ff01f...`, равный `README.md` актуального `main`; посторонних правок нет.

### Блокеры / найденные риски
- В текущей macOS-среде нет Docker CLI и PowerShell: настоящие контейнерные прогоны на чистой/поднятой машине и исполнение `.ps1` здесь невозможны. Оркестрация всех сценариев проверена изолированно, PowerShell — статически; реальный Windows/Docker smoke остаётся ручной проверкой перед принятием PR.
- Разрушающих действий в мастерах нет, поэтому подтверждение `YES` не запрашивается; если такие действия появятся позже, правило обязательного полного `YES` должно быть сохранено.

### Точный следующий шаг
Создать ветку `task/T-017`, один коммит и PR без merge; на машине с Docker/Windows повторить реальные пять сценариев.

### SUBAGENT_HANDOFF
Субагенты не используются.
