## Задача: T-010 (wiring Login/Register в приложение)
Исполнитель: frontend
Начато: 20260906T171715Z
Предыдущая попытка: docs/agent/tasks/T-010/20260906T162322Z_frontend_a01.md (срез auth UI: README, Login, Register, Auth.test)

### Цель и разрешённый scope
Только фронт: создать `frontend/src/App.tsx` (роутер по состоянию без react-router: /login, /register, /queues через MyQueues, редирект по роли после входа — /teacher заглушка «скоро», student → /queues; токен из localStorage); отредактировать `frontend/src/main.tsx` (рендерить App вместо голого MyQueues); тест рядом `App.test.tsx` (редирект без токена → /login).
Не тронуты: `backend/*`, `tests/*`, `TASKS.md`, `MyQueues.tsx`, `Login.tsx`, `Register.tsx` (только импортированы). Ветка: `task/T-010-wiring`.

### Просмотренные файлы (только чтение)
- frontend/src/main.tsx (было: голый MyQueues с токеном из localStorage)
- frontend/src/screens/MyQueues.tsx (пропсы accessToken/apiBaseUrl/initialQueues/onOpenQueue)
- frontend/src/screens/Login.tsx (пропсы apiBaseUrl/onSuccess(role); роль из payload JWT)
- frontend/src/screens/Register.tsx (пропсы apiBaseUrl/onSuccess(user))

### Изменённые файлы (2 новых + 1 правка)
- `frontend/src/App.tsx` (новый: AppRoute, readStoredToken, routeForRole, защищённые маршруты без токена → /login, nav-кнопки Вход/Регистрация, заглушка «Кабинет преподавателя — скоро»)
- `frontend/src/main.tsx` (правка: рендерит <App/>; чтение localStorage переехало в App)
- `frontend/src/App.test.tsx` (новый: 4 теста)
- этот файл попытки

### Команды и результаты
- `docker compose run --rm frontend npm test -- --run` → первый прогон упёрся в занятый порт 8000 (зависимость backend не смогла пересоздаться из-за старого t001-контейнера); повтор с `--no-deps` → 3 файла, 14 passed (MyQueues 1 + Auth 9 + App 4).
- `docker compose run --rm frontend npm run build` → tsc + vite OK (31 модуль).
- Backend-тесты не запускались (запрет).
- Прибран лишний `project-docs-frontend-1`, поднятый первым прогоном как зависимость; чужие контейнеры (t001) не тронуты.

### Блокеры / найденные риски
- БЛОКЕР: GitHub-интеграция недоступна в этом окружении. Прямые git-команды в обход запрещены, поэтому коммит в ветку `task/T-010-wiring` и PR в main НЕ выполнены. Требуется действие человека: закоммитить App.tsx, main.tsx, App.test.tsx и этот отчёт, открыть PR без merge.

### Точный следующий шаг
- Человеку: закоммитить 4 файла в task/T-010-wiring, открыть PR без merge; далее — экран очереди студента и кабинет преподавателя вместо заглушки.

### SUBAGENT_HANDOFF
Субагенты не используются.
