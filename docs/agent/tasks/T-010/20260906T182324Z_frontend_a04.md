## Задача: T-010 (Auth: карточка выше, сегмент-переключатель, крупный логотип)
Исполнитель: frontend
Начато: 20260906T182324Z
Предыдущая попытка: docs/agent/tasks/T-010/20260906T180325Z_frontend_a03.md (рестайл Material You)

### Цель и разрешённый scope
Только фронт: 1) карточка выше центра (flex-start + margin-top 13vh десктоп / 6vh мобиле); 2) кнопки Вход/Регистрация из шапки — в карточку синим сегментом (активная залита #2F7CF6, неактивная прозрачная), нав App на этих роутах скрыт; 3) логотип «Умная очередь» крупнее (31px, трекинг 0.01em), минималистичный текст; 4) синий переключателя спокойный #2F7CF6, белый текст на активном.
Не менялись: API-контракты, пропсы Login/Register (навигация вкладок — через событие `auth:route` без новых пропсов), валидация после submit, остальная палитра.
Файлы: Auth.css, Login.tsx, Register.tsx, AuthTabs.tsx (новый), App.tsx, Auth.test.tsx, App.test.tsx. Не тронуты: `backend/*`, `tests/*` (бэк), `TASKS.md`, `MyQueues.tsx`. Ветка: `task/T-010-design2`.

### Изменённые файлы
- `frontend/src/screens/Auth.css` (позиция карточки, крупный логотип, .auth-tabs/.auth-tab/.auth-tab--active, reduced-motion)
- `frontend/src/screens/AuthTabs.tsx` (новый: AuthTabs + emitAuthRoute + AUTH_ROUTE_EVENT)
- `frontend/src/screens/Login.tsx` / `Register.tsx` (только импорт и <AuthTabs active>)
- `frontend/src/App.tsx` (слушатель auth:route в useEffect; блок <nav> удалён — дублей нет)
- `frontend/src/screens/Auth.test.tsx` (+4: позиция/логотип, активная вкладка входа/регистрации, событие навигации)
- `frontend/src/App.test.tsx` (+1: нет <nav> на auth-роутах, сегмент на месте)
- этот файл попытки

### Команды и результаты
- `docker compose run --rm --no-deps frontend npm test -- --run` → 3 файла, 22 passed (было 17: +4 Auth, +1 App).
- `docker compose run --rm --no-deps frontend npm run build` → tsc + vite OK (33 модуля).
- Backend-тесты не запускались (запрет).

### Блокеры / найденные риски
- БЛОКЕР: GitHub-интеграция недоступна в этом окружении. Прямые git-команды в обход запрещены, поэтому коммит в ветку `task/T-010-design2` и PR в main НЕ выполнены. Требуется действие человека: закоммитить 8 файлов и открыть PR без merge.

### Точный следующий шаг
- Человеку: закоммитить Auth.css, AuthTabs.tsx, Login.tsx, Register.tsx, App.tsx, Auth.test.tsx, App.test.tsx и этот отчёт в task/T-010-design2, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
