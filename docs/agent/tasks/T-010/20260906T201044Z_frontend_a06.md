## Задача: T-010 (полировка: дедуб JWT, Auth.css на токенах, аудит роутера)
Исполнитель: frontend
Начато: 20260906T201044Z
Предыдущая попытка: docs/agent/tasks/T-010/20260906T195811Z_frontend_a05.md (единая тема)

### Цель и разрешённый scope
Фронт-полировка в рамках закрытого T-010 (статус/чекбоксы не трогал): 1) убрать дубль JWT-декода — оставлена версия из `frontend/src/auth/session.ts` (с padEnd), из Login.tsx удалены re-export шимы, все места используют общую; 2) Auth.css переведён на токены theme.css (только точные совпадения значений); 3) проверено покрытие App-роутера. Запрещены: `backend/*`, `tests/*`, TASKS.md, PRODUCT.md, новые экраны/зависимости. Ветка: `task/T-010-polish`.

### Изменённые файлы
- `frontend/src/screens/Login.tsx` (удалены 2 строки re-export; импорты/компонент/валидация без изменений)
- `frontend/src/screens/Register.tsx` (тип UserRole теперь из ../auth/session)
- `frontend/src/screens/Auth.test.tsx` (decodeRoleFromToken/saveTokens теперь из ../auth/session)
- `frontend/src/screens/Auth.css` (@import theme.css; var() для bg/font/ink/surface/radius-2xl/shadow-lifted/primary/accent/radius-lg/radius-md)
- этот файл попытки

### Решения
- Дубль был уже частично сведён мержем основной (Login импортировал общую версию); остались только шимы — удалены, тесты переведены на прямой импорт.
- В Auth.css токенизированы только значения, совпадающие с theme 1:1; буквально оставлены #e8eff9, #5a6b87, #8ba0bd, invalid-цвета, danger-цвета Auth (#c74336/#fff1ef/#ffd9d4 — отличаются от theme-danger), hover #f25a4d, радиус 14px, rgba-тени — визуал не сдвинулся ни на пиксель.
- Аудит App-роутера чтением: /login, /register, /queues, /queue (+фолбэк без selectedQueueId), /settings, /teacher, /teacher/session (+фолбэк) — все 7 значений AppRoute имеют ветви; все навигации (onOpenQueue/onBack/onSettings/onDeleted/onOpenSession/auth-событие/роль) ведут в существующие; мёртвых роутов нет — правок не потребовалось.

### Команды и результаты
- `docker compose run --rm --no-deps frontend npm test -- --run` → 10 файлов, 40 passed (snapshot-тексты/классы целы).
- `docker compose run --rm --no-deps frontend npm run build` → tsc + vite OK (44 модуля).
- Backend-тесты не запускались (запрет).

### Блокеры / найденные риски
- БЛОКЕР: GitHub-интеграция недоступна в этом окружении. Прямые git-команды в обход запрещены, поэтому коммит в ветку `task/T-010-polish` и PR в main НЕ выполнены. Требуется действие человека: закоммитить 5 файлов и открыть PR без merge.

### Точный следующий шаг
- Человеку: закоммитить Login.tsx, Register.tsx, Auth.test.tsx, Auth.css и этот отчёт в task/T-010-polish, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
