## Задача: кнопки «выше/ниже/в начало» только на узких тач-экранах
Исполнитель: frontend
Начато: 20260907T145948Z
Предыдущая попытка: docs/agent/tasks/T-010/20260907T070800Z_frontend_a08.md (reaudit)

### Цель и scope
Только CSS-видимость + тест. Логику moveOwnEntry/rollback не трогал
(Queue.tsx вообще не менялся).

### Изменённые файлы
- frontend/src/components/student/Student.css: `.student-reorder-controls`
по умолчанию `display:none`; показ только в
`@media (max-width:640px) and (pointer:coarse)`. Разметка в DOM остаётся
(скринридеры, drag на десктопе — основной).
- frontend/src/screens/Queue.test.tsx: +2 теста (десктоп: `display:none`
в CSS + кнопки в разметке + `draggable="true"` у своей; тач-показ в медиа).
Попытка через `?raw`-импорт CSS не сработала (vitest отдал пустую строку) —
тест читает файл через node:fs, для типов добавлен dev `@types/node`.
- frontend/package.json + package-lock.json (`@types/node`, только для tsc).
- этот файл попытки.

### Команды и результаты
- `npm test -- --run` → 14 файлов, 64 passed (62 старых + 2 новых).
- `npm run build` → tsc + vite OK.

### Блокеры
- GitHub-интеграция недоступна: коммит и PR без merge не выполнены.
Требуется человек.

### SUBAGENT_HANDOFF
Субагенты не используются.
