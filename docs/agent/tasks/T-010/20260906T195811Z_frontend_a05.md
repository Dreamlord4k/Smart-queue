## Задача: T-010 (единая Material You-тема фронта)
Исполнитель: frontend
Начато: 20260906T195811Z
Предыдущая попытка: docs/agent/tasks/T-010/20260906T182324Z_frontend_a04.md (Auth-позиция и сегмент)

### Цель и разрешённый scope
Единая тема без конфликта с основной (Queue.tsx/Settings.tsx — не трогал, их нет в работе). Разрешены только: `frontend/src/theme.css` (новый: переменные #F2F6FC/#FFFFFF/#2F7CF6/#0B1B33, коралл #FF6B5E, радиусы 16–28px, тени, шрифт-стек), `frontend/src/screens/MyQueues.tsx` (инлайн-стили → классы темы), `frontend/src/components/teacher/Teacher.css` (те же токены, визуал тот же), тесты рядом, этот отчёт. Запрещены: `backend/*`, `tests/*`, Queue/Settings, Login/Register/Auth.css/App, TASKS.md. Логика/пропсы/тексты не менялись — только классы. Ветка: `task/T-010-theme`.

### Изменённые файлы
- `frontend/src/theme.css` (новый: :root-переменные + классы .mq-*)
- `frontend/src/screens/MyQueues.tsx` (import ../theme.css; 5 инлайн-стилей → mq-page/mq-grid/mq-card/mq-warn/mq-error/mq-button)
- `frontend/src/components/teacher/Teacher.css` (@import theme.css первой строкой; hex → var() с теми же значениями)
- `frontend/src/screens/MyQueues.test.tsx` (+1 тест классов темы, старый тест цел)
- этот файл попытки

### Решения
- theme.css грузится через import из MyQueues и @import из Teacher.css — main.tsx/App трогать не пришлось.
- В Teacher.css токенизировано всё из общей палитры; буквально оставлены только уникальные значения (#174477, #24558b, радиусы 14/15px) — вычисляемый визуал 1:1.
- Auth.css не тронут: его хардкод уже совпадает с палитрой, подтянется отдельной задачей при желании.

### Команды и результаты
- `docker compose run --rm --no-deps frontend npm test -- --run` → 6 файлов, 29 passed (включая новый тест классов).
- `docker compose run --rm --no-deps frontend npm run build` → tsc + vite OK (44 модуля, общий CSS-бандл).
- Backend-тесты не запускались (запрет).

### Блокеры / найденные риски
- БЛОКЕР: GitHub-интеграция недоступна в этом окружении. Прямые git-команды в обход запрещены, поэтому коммит в ветку `task/T-010-theme` и PR в main НЕ выполнены. Требуется действие человека: закоммитить 5 файлов и открыть PR без merge.
- Риск слияния низкий: Queue.tsx/Settings.tsx не созданы ни у меня, ни в main на момент работы; TeacherDashboard/SessionForm (tsx основной) не тронуты.

### Точный следующий шаг
- Человеку: закоммитить theme.css, MyQueues.tsx, Teacher.css, MyQueues.test.tsx и этот отчёт в task/T-010-theme, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
