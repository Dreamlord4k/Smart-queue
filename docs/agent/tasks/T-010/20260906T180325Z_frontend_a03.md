## Задача: T-010 (рестайл Login/Register в Material You)
Исполнитель: frontend
Начато: 20260906T180325Z
Предыдущая попытка: docs/agent/tasks/T-010/20260906T171715Z_frontend_a02.md (wiring App)

### Цель и разрешённый scope
Только фронт: рестайл Register + Login в стиле Material You (Pixel/Android 17), API-контракты не менять. Светлая тема, скругления 16–20px+, мягкие тени; палитра #F2F6FC/#FFFFFF/#2F7CF6/#0B1B33, коралл #FF6B5E только submit/ошибки/фокус-подчерк; системный шрифт без CDN; текстовый логотип «Умная очередь»; bounce кнопки 130ms + фокус-ринг. Валидация после нажатия (email без @, пустой/короткий пароль — коралловые подсказки под полями); успех — редирект как раньше. Пропсы/контракты (apiBaseUrl, onSuccess, localStorage) и endpoint-ы сохранены.
Не тронуты: `backend/*`, `tests/*` (бэк), `TASKS.md`, `App.tsx`, `MyQueues.tsx`. Ветка: `task/T-010-design`.

### Изменённые файлы (1 новый + 3 правки)
- `frontend/src/screens/Auth.css` (новый: тема, карточка, поля, коралловая кнопка, фокус-подчерк, reduced-motion)
- `frontend/src/screens/Login.tsx` (правка: классы, validateLoginInput, noValidate, подсказки; тексты/пропсы/endpoint без изменений)
- `frontend/src/screens/Register.tsx` (правка: классы, validateRegisterInput, noValidate, подсказки; поле группы только для student как раньше)
- `frontend/src/screens/Auth.test.tsx` (правка: +3 теста валидации/стиля, старые тексты не менялись)
- этот файл попытки

### Команды и результаты
- `docker compose run --rm --no-deps frontend npm test -- --run` → 3 файла, 17 passed (было 14: MyQueues 1 + App 4 + Auth 9; стало Auth 12).
- `docker compose run --rm --no-deps frontend npm run build` → tsc + vite OK (32 модуля, отдельный CSS-бандл 2.07 kB).
- Backend-тесты не запускались (запрет).

### Блокеры / найденные риски
- БЛОКЕР: GitHub-интеграция недоступна в этом окружении. Прямые git-команды в обход запрещены, поэтому коммит в ветку `task/T-010-design` и PR в main НЕ выполнены. Требуется действие человека: закоммитить Auth.css, Login.tsx, Register.tsx, Auth.test.tsx и этот отчёт, открыть PR без merge.

### Точный следующий шаг
- Человеку: закоммитить 5 файлов в task/T-010-design, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
