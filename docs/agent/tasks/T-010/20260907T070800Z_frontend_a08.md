## Задача: reaudit всего фронта на остатки старого интерфейса
Исполнитель: frontend
Начато: 20260907T070800Z
Предыдущая попытка: docs/agent/tasks/T-010/20260907T062624Z_frontend_a07.md (M3 Expressive)

### Цель и разрешённый scope
Перепроверить весь frontend; найденный старый интерфейс — обновить.
Не тронуты: backend/*, TASKS.md, T-010 статус.

### Методика
Grep-аудит: инлайн-стили в tsx, точки «·», старая палитра (#2F7CF6/#F2F6FC/#0B1B33/#FF6B5E/#D97757),
старые имена токенов, hex вне theme.css, fontFamily/sans-serif, text-transform/uppercase/«→»,
покрытие классов (все className из tsx против определений в CSS), чтение изменившихся файлов.

### Найдено и исправлено (3 шт.)
1. Регрессия: потерян `body{margin:0}` из старого Teacher.css → браузерные 8px поля
вокруг страниц + белый фон body. Возвращено в theme.css (единый владелец базы).
2. `index.html`: добавлен `<meta name="theme-color" content="#fffbfa">`.
3. Класс `theme-toggle__label` без CSS-правила → добавлено `white-space: nowrap`.
Проверено и чисто без правок: инлайн-стили (0), «·» (0), старая палитра (0),
старые токены (0 — совпадение по shadow-* ложное, имена общие), шрифты (0),
ALL-CAPS/стрелки (0). Register за время мержа переехал на select групп —
уже на классах темы, не трогал.

### Изменённые файлы
- frontend/src/theme.css (body-база + .theme-toggle__label)
- frontend/index.html (theme-color meta)
- этот файл попытки

### Команды и результаты
- `npm test -- --run` → 13 файлов, 50 passed.
- `npm run build` → tsc + vite OK.

### Блокеры
- GitHub-интеграция недоступна: коммит и PR не выполнены. Требуется человек.

### Точный следующий шаг
- Человеку: закоммитить 3 файла, при желании отдельным мелким PR.

### SUBAGENT_HANDOFF
Субагенты не используются.
