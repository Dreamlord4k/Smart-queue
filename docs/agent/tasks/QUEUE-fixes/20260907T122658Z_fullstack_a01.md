## Задача: QUEUE-fixes (FIX-005 + FIX-006 + queue UI/reorder)
Исполнитель: fullstack
Начато: 20260907T122658Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Одним небольшим PR исправить мобильное управление собственной записью, убрать сдвиг списка при раскрытии причины фиксации, исправить optimistic reorder/rollback и сохранение locked-позиций, устранить нулевую ширину ETA до первого наблюдения, а также развести кнопки преподавателя на мобильном.

По прямому решению пользователя применяется исключение из стандартного ритуала: три связанных исправления объединяются в одну ветку `task/QUEUE-fixes`, один PR и один итоговый отчёт без отдельных FIX-коммитов и файлов попыток.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `backend/AGENTS.md`, `tests/AGENTS.md`
- ETA, backend reorder, студенческий и преподавательский экраны, стили и тесты
- актуальные версии затрагиваемых файлов в GitHub `main`

### Решения пользователя
- Сохраняется семантика вставки `before/after`: 5 перед 3 даёт `[1,2,5,3,4]`.
- Целевой locked-слот по-прежнему запрещён; при пересечении locked-позиция остаётся абсолютной, переставляются незакреплённые записи.
- При свободных параллельных каналах одинаковое начало ETA ожидаемо; исправляется только нулевая ширина диапазона до первого наблюдения.

### Изменённые файлы
- `backend/queue/eta.py` — ненулевая априорная ширина ETA до появления статистики.
- `backend/routes/queue.py` — перестановка только по свободным слотам с сохранением абсолютных locked-позиций.
- `tests/queue/test_eta.py`, `tests/queue/test_reorder.py` — регрессии ETA и crossing locked-якоря.
- `frontend/src/screens/Queue.tsx`, `frontend/src/screens/Queue.test.tsx` — корректная insertion-семантика, точный rollback, автопрокрутка и мобильные кнопки, drawer причины фиксации.
- `frontend/src/screens/TeacherSession.tsx`, `frontend/src/screens/TeacherSession.test.tsx` — отдельный класс группы действий канала.
- `frontend/src/components/student/Student.css`, `frontend/src/components/teacher/Teacher.css` — drawer и мобильная раскладка кнопок без наложения.
- `docs/TECH_SPEC.md` — явно описаны параллельные каналы и априорная неопределённость.
- этот итоговый отчёт.

### Команды и результаты
- Красный ETA-тест до исправления: `1 failed, 4 passed`; воспроизведён нулевой диапазон при `capacity=10`, четырёх waiting и отсутствии статистики.
- После исправления ETA: `5 passed`.
- Красный reorder-тест до исправления: crossing locked-якоря возвращал `409`.
- После исправления `tests/queue/test_reorder.py`: `7 passed`.
- Полный `tests/queue` локально на SQLite: `22 passed, 1 failed`; единственный сбой — ранее известный конкурентный тест, которому нужны PostgreSQL row locks. Авторитетная проверка — GitHub Actions с PostgreSQL.
- Точечные UI-тесты: `10 passed`.
- Полная фронтенд-регрессия: `54 passed`.
- Production build фронтенда: успешно.

### Блокеры / найденные риски
- Локальная среда не содержит PostgreSQL, поэтому конкурентный backend-тест подтверждается в Actions.
- Реальную очередь на viewport 375 px локально без API/seed не поднять; fallback-кнопки и fixed drawer покрыты тестами, финальная ручная проверка остаётся на стенде после деплоя.

### Точный следующий шаг
Показать diff, открыть один PR из `task/QUEUE-fixes` без merge и дождаться зелёного GitHub Actions на PostgreSQL.

### SUBAGENT_HANDOFF
Субагенты не используются.
