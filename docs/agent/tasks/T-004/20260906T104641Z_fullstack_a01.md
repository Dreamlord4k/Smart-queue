## Задача: T-004
Исполнитель: fullstack
Начато: 20260906T104641Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Реализовать отказ студента от конкретной сессии без удаления записи, нормализацию активной очереди и пересчёт ETA, а также главный экран студента «Мои очереди» с независимыми карточками сессий и предупреждением о пересечении.

Исходный scope из TASKS.md: `backend/models/queue_entry.py`, `backend/routes/queue.py`, `backend/queue/eta.py`, `frontend/src/screens/MyQueues.tsx`, `tests/queue/*`.

Пользователь дополнительно разрешил необходимые папки без отдельного согласования. Для запуска маршрутов и отсутствующего frontend-каркаса scope расширен на `backend/main.py` и минимальные файлы `frontend/`.

### Просмотренные файлы
- `AGENTS.md`
- `PRODUCT.md`
- `PLAN.md`
- `TASKS.md`
- `docs/TECH_SPEC.md`
- `backend/AGENTS.md`
- `tests/AGENTS.md`
- `docs/agent/tasks/T-001/README_TEMPLATE.md`

### Изменённые файлы
- `.gitignore`
- `backend/__init__.py`
- `backend/Dockerfile`
- `backend/main.py`
- `backend/queue/eta.py`
- `backend/routes/queue.py`
- `docker-compose.yml`
- `frontend/.dockerignore`
- `frontend/Dockerfile`
- `frontend/index.html`
- `frontend/package.json`
- `frontend/package-lock.json`
- `frontend/tsconfig.json`
- `frontend/vite.config.ts`
- `frontend/src/main.tsx`
- `frontend/src/vite-env.d.ts`
- `frontend/src/screens/MyQueues.tsx`
- `frontend/src/screens/MyQueues.test.tsx`
- `tests/queue/conftest.py`
- `tests/queue/test_absence.py`
- этот файл попытки

### Команды и результаты
- `docker compose run --rm backend pytest /tests/queue -q` -> `4 passed`.
- `docker compose run --rm frontend npm test` -> `1 test passed`.
- `docker compose run --rm frontend npm run build` -> production-сборка успешна.
- `docker compose run --rm backend pytest /tests/auth /tests/sessions /tests/queue -q` -> `14 passed`.
- HTTP smoke-проверка `GET /health` и главной страницы frontend -> оба сервиса отвечают успешно.
- Первый запуск выявил конфликт имени локального пакета `queue` со стандартным модулем Python; backend переведён на пакетный импорт без переименования требуемого `backend/queue/eta.py`.
- Первый целевой прогон после реализации: `1 failed, 3 passed` из-за отключённого SQLAlchemy autoflush; добавлен явный `flush` внутри транзакции, повторный прогон успешен.

### Блокеры / найденные риски
- Блокеров нет.
- Поля `status` и `absence_reason` уже созданы в T-003; новая миграция для них не потребовалась.
- До появления статистики фактического обслуживания live-ETA использует `duration_default`; функция уже учитывает `capacity` и занятые каналы.
- Pytest сообщает только внешние предупреждения Starlette/httpx и о read-only каталоге кэша тестов; на результат проверок они не влияют.

### Точный следующий шаг
Создать ветку `task/T-004`, коммит и Pull Request; после проверки и мержа человеком основной агент сможет отметить T-004 как DONE.

### SUBAGENT_HANDOFF
Субагенты не используются.
