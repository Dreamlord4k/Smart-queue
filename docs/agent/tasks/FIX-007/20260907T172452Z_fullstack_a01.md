## Задача: FIX-007 — демо-панель, часовой пояс и ETA после добавления участника
Исполнитель: fullstack
Начато: 20260907T172452Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Скрыть демо-панель для авторизованных, исправить planned ETA для локального времени Екатеринбурга и отображение часового пояса на фронте, локализовать и исправить пересчёт ETA всей очереди после добавления участника.

Ветка: `fix/FIX-007`. Один коммит и один PR для пунктов 1–3 по прямому указанию пользователя. `TASKS.md` не изменяется.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `backend/AGENTS.md`, `tests/AGENTS.md`

### Изменённые файлы
- `frontend/src/components/DemoPanel.tsx` и тест — панель рендерится только гостю; скрытый компонент сохраняет 30-минутный автосброс demo-сессии.
- `frontend/src/utils/time.ts` и тест — единый форматтер `Asia/Yekaterinburg` и подпись «время местное».
- `frontend/src/screens/MyQueues.tsx`, `Queue.tsx`, `TeacherDashboard.tsx`, `TeacherSession.tsx` — локальное отображение времени.
- `backend/queue/eta.py` — planned ETA получает `+05:00`; active ETA использует окна обслуживания по реальным каналам.
- `backend/routes/sessions.py` — явная нормализация всей активной очереди после добавления участника.
- `tests/queue/test_eta.py`, `tests/queue/test_absence.py`, `tests/sessions/test_session_management.py` — регрессии часового пояса, непрерывных окон и realtime-события.
- этот файл попытки.

### Команды и результаты
- Frontend: 6 файлов / 23 теста — passed; `npm run build` — passed.
- Backend targeted: `test_eta.py`, `test_absence.py`, `test_session_management.py` — 16 passed.
- Полный локальный `tests/queue tests/sessions`: 38 passed, 6 failed до обновления timezone-ожидания; после исправления релевантные тесты зелёные. Оставшиеся 5 — известная несовместимость строковых UUID тестов с SQLite; окончательная проверка выполняется CI на PostgreSQL.
- `python -m compileall` для изменённых backend/tests файлов — passed.

### Блокеры / найденные риски
- Для Python используется валидный IANA-идентификатор `Asia/Yekaterinburg` (UTC+5); формулировка «Europe/Yekaterinburg» трактуется как екатеринбургская зона, а не несуществующий IANA ID.
- Локально нет Docker/PostgreSQL; пять известных SQLite-only падений не относятся к FIX-007. PR нельзя считать принятым до зелёного CI на PostgreSQL.

### Точный следующий шаг
Создать один коммит в `fix/FIX-007`, открыть PR без merge и дождаться PostgreSQL CI.

### SUBAGENT_HANDOFF
Субагенты не используются.
