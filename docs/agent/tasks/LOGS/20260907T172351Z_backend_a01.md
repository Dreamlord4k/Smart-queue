## Задача: структурированное логирование операций очереди (FIX-005)
Исполнитель: backend+frontend
Начато: 2026-09-07T17:23:51Z
Предыдущая попытка: нет

### Цель и scope
Только добавление строк, логику не менять. Бэк: JSON одной строкой
(ts/session_id/entry_id/actor_id/op/позиции/EMA), ошибки — exception
с traceback. Фронт: console.error({url без query, status, action}).
Файлы: backend/logging_config.py (новый), backend/routes/queue.py,
backend/routes/sessions.py (только строки), frontend api teacher/student,
тесты рядом. TASKS.md не тронут. Ветка: task/LOGS.

### Изменённые файлы
- `backend/logging_config.py` (новый: JsonFormatter, get_queue_logger,
log_operation, log_route_errors, FORBIDDEN_LOG_KEYS)
- `backend/routes/queue.py` (info в done/skip/reorder/lock/absence +
декораторы ошибок на 5 эндпоинтах; чтения ema_before/position/locked —
read-only, поведения не меняют)
- `backend/routes/sessions.py` (info в add/remove/freeze + декораторы)
- `frontend/src/api/teacher.ts`, `frontend/src/api/student.ts`
(request с action, console.error; исходные ошибки пробрасываются как были)
- `tests/queue/test_operation_logs.py` (4 caplog-теста)
- `frontend/src/api/teacher.test.ts` (+1 тест console.error)
- этот файл попытки

### Секреты
Grep: в лог-вызовах нет access_token/password/bearer; хелпер отбрасывает
запрещённые ключи даже при ошибочной передаче. 4xx (HTTPException) в
traceback не пишутся — только неожиданные исключения (re-raise, поведение то же).

### Пример реальных строк (захвачены прогоном, id сокращены)
```json
{"ts": "2026-09-07T17:21:42+00:00", "level": "INFO", "logger": "smart_queue.queue", "op": "session.frozen", "session_id": "12ca1db5…", "actor_id": "a8c06c99…", "frozen_before": false, "frozen_after": true}
{"ts": "2026-09-07T17:21:42+00:00", "level": "INFO", "logger": "smart_queue.queue", "op": "queue.absent", "session_id": "e553983a…", "entry_id": "572925db…", "actor_id": "d7df10fb…", "position_before": 2, "status_before": "waiting", "released_channel": null}
{"ts": "2026-09-07T17:21:42+00:00", "level": "INFO", "logger": "smart_queue.queue", "op": "queue.reordered", "session_id": "381f70a7…", "entry_id": "a6746509…", "actor_id": "5b320069…", "position_before": 3, "position_after": 1}
```

### Команды и результаты
- Новые тесты: `/tests/queue/test_operation_logs.py` → 4 passed.
- Backend `/tests` (со scripts-маунтом из README): 64 passed, 2 failed —
оба в `tests/infra` (читают `/scripts/deploy/...`, отсутствующий в этом
маунте); предсущие, моих файлов не касаются.
- Frontend: 14 файлов, 65 passed (64 + 1 новый); `npm run build` OK.
- Нужна была пересборка backend-образа (новый модуль).

### Блокеры
- GitHub-интеграция недоступна: коммит в `task/LOGS` и PR без merge
не выполнены. Требуется человек.

### Точный следующий шаг
- Человеку: закоммитить пакет в task/LOGS, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
