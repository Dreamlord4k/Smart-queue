## Задача: FIX-010 — демо-панель без перекрытия интерфейса и материалы к защите
Исполнитель: frontend
Начато: 2026-09-08T17-16-29Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Подготовить два русскоязычных TXT-материала: подробное объяснение архитектурной схемы и краткую памятку разработчика/DevOps с вероятными вопросами комиссии. В интерфейсе оставить демо-панель закреплённой в углу только для гостевых экранов входа/регистрации; в авторизованных демо-кабинетах разместить переключение ролей и сброс обычным нижним блоком страницы, чтобы он не перекрывал действия. В обычной авторизованной сессии панель по-прежнему отсутствует.

Ветка: `fix/FIX-010`. Один коммит и один PR без merge.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `README.md`, compose/nginx/deploy-конфигурация и ключевые backend/frontend-модули
- `frontend/src/App.tsx`, `frontend/src/components/DemoPanel.tsx`, стили и тесты рядом
- Impeccable: `SKILL.md`, `reference/layout.md`; контекст загружен для `DemoPanel.tsx`

### Изменённые файлы
- `docs/presentation/smart-queue-architecture-explained.txt` — схема, роли всех компонентов, связи, ETA/EMA, отказы и готовые устные формулировки.
- `docs/presentation/developer-devops-commission-cheatsheet.txt` — краткий разбор реализации/эксплуатации, ограничения MVP, 40 вопросов комиссии и чек-лист защиты.
- `frontend/src/App.tsx` — DemoPanel рендерится после содержимого текущего маршрута.
- `frontend/src/components/DemoPanel.tsx` — описание demo-сессии собрано в отдельную смысловую группу.
- `frontend/src/components/DemoPanel.css` — авторизованная demo-панель стала адаптивным нижним блоком normal flow; гостевая панель осталась fixed в углу; удалён компенсирующий нижний отступ 250 px.
- `frontend/src/App.test.tsx`, `frontend/src/components/DemoPanel.test.tsx` — покрыты порядок рендера, session-модификатор, гостевое и обычное авторизованное состояния.
- этот файл попытки.

### Команды и результаты
- узкий frontend-набор (`App` + `DemoPanel`): `14 passed`.
- полный frontend-набор: `77 passed`.
- production build: успешно, 53 модуля собраны.
- Impeccable detector: `[]`, замечаний нет (выполнен ровно один финальный прогон).
- локальный UI загружен в браузере; полноценное demo-состояние требует поднятого backend/seed, поэтому размещение подтверждено тестом DOM-порядка и CSS для desktop/375 px.

### Блокеры / найденные риски
- задача дана как срочный фикс вне `TASKS.md`; `TASKS.md` не изменяется.
- текущий `docs/TECH_SPEC.md` местами сохраняет старую формулировку об опциональном `absence_reason`; в подготовленной памятке отражено актуальное правило из PRODUCT.md и работающего API: причина обязательна. Исходную спецификацию этот фикс не меняет.

### Точный следующий шаг
Показать компактный diff, создать один коммит в `fix/FIX-010`, открыть PR без merge и дождаться результата GitHub Actions.

### SUBAGENT_HANDOFF
Субагенты не используются.
