## Задача: T-002
Исполнитель: backend
Начато: 2026-09-06T09:59:56Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Реализовать регистрацию и вход по уникальному email, хэширование пароля Argon2, JWT access+refresh, обновление access без повторного пароля, серверное ограничение teacher-only действий по роли и удаление собственного профиля.

Исходный scope из TASKS.md: `backend/auth/*`, `backend/routes/auth.py`, `tests/auth/*`.

Согласованное с пользователем расширение: `backend/main.py`, `backend/requirements.txt`, `docker-compose.yml`, `backend/models/user.py`, `backend/db/migrations/*`. Этот файл попытки является служебным исключением из продуктового scope.

### Просмотренные файлы
- `AGENTS.md`
- `PRODUCT.md`
- `PLAN.md`
- `TASKS.md`
- `docs/TECH_SPEC.md`
- `backend/AGENTS.md`
- `tests/AGENTS.md`
- `docs/agent/tasks/T-001/20260906T093011Z_backend_a01.md`

### Изменённые файлы
- `backend/models/user.py`
- `backend/db/migrations/versions/20260906_0002_user_email.py`
- `backend/auth/config.py`
- `backend/auth/security.py`
- `backend/auth/schemas.py`
- `backend/auth/dependencies.py`
- `backend/routes/auth.py`
- `backend/main.py`
- `backend/requirements.txt`
- `docker-compose.yml`
- `tests/auth/conftest.py`
- `tests/auth/test_auth.py`
- `docs/agent/tasks/T-002/20260906T095956Z_backend_a01.md`

### Команды и результаты
- GitHub-интеграция: PR #1 смёржен после явного подтверждения пользователя; merge commit `b1c3a3c2ffeb5562713e4637cf669cdeadc1c0fd`.
- GitHub-интеграция: `TASKS.md` в `main` обновлён — T-001 отмечена как `DONE`, проверки `2/2`.
- проверка синтаксиса Python через `ast.parse` -> успешно, 13 файлов;
- разбор `docker-compose.yml` YAML-парсером -> успешно;
- первый запуск Docker-стенда -> обнаружены занятые внешними процессами порты 5432 и 8000; публикация PostgreSQL наружу удалена как ненужная, порт backend параметризован через `BACKEND_PORT` с default 8000;
- `docker compose ... up --build -d` на чистом изолированном стенде -> PostgreSQL `healthy`, backend запущен;
- Alembic -> применены ревизии `20260906_0001` и `20260906_0002`;
- проверка схемы `users` -> `email varchar(320) NOT NULL`, constraint `uq_users_email UNIQUE`;
- первый вызов консольного `pytest` -> импорт backend не найден из `/tests`; команда исправлена на `python -m pytest` из `/app`, продуктовый код не менялся;
- `python -m pytest -q -p no:cacheprovider /tests/auth` -> `5 passed`, 2 предупреждения о deprecated API в сторонней связке Starlette/TestClient;
- `GET http://localhost:18000/health` -> `{"status":"ok"}`;
- `docker compose ... down -v` -> удалены только тестовые контейнеры, сеть и том T-002.
- GitHub-интеграция -> создан коммит `5a9bf645113dde1a3b13b23ab2c10352120f9686` в `task/T-002` и открыт Pull Request [#2](https://github.com/Dreamlord4k/Smart-queue/pull/2) в `main`; merge не выполнялся.

### Блокеры / найденные риски
- Блокеров нет. Два предупреждения pytest относятся к зависимостям Starlette/TestClient и не влияют на проверки T-002.

### Точный следующий шаг
Проверить Pull Request #2 человеку; после одобрения основной агент может смёржить PR и обновить статус T-002 в `TASKS.md`.

### SUBAGENT_HANDOFF
Субагенты не привлекались.
