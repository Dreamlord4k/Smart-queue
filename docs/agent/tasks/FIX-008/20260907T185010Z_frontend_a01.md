## Задача: FIX-008 — управление демо и заморозкой
Исполнитель: frontend
Начато: 20260907T185010Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Вернуть компактную панель со сбросом для авторизованной demo-сессии и сделать управление заморозкой порядка симметричным во всех незавершённых состояниях сессии.

Ветка: `fix/FIX-008`. `TASKS.md` не изменяется.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `frontend/src/components/DemoPanel.tsx` и тест
- `frontend/src/screens/TeacherSession.tsx` и тест

### Изменённые файлы
- `frontend/src/components/DemoPanel.tsx` и тест — demo-сессия сохраняет компактный индикатор, ручной сброс и 30-минутный автосброс; обычный авторизованный пользователь панель не видит.
- `frontend/src/screens/TeacherSession.tsx` и тест — единый переключатель «Заморозить список»/«Разморозить» для planned, active и paused.
- этот файл попытки

### Команды и результаты
- Целевые UI-тесты: 12 passed.
- Полный frontend: 15 файлов, 72 теста — passed.
- Production build — passed.

### Блокеры / найденные риски
- Нет.

### Точный следующий шаг
Создать один коммит в `fix/FIX-008`, показать diff, открыть PR без merge и дождаться CI.

### SUBAGENT_HANDOFF
Субагенты не используются.
