## Задача: FIX-001
Исполнитель: devops
Начато: 20260906T141317Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Стабилизировать автономную сборку backend и сетевую конфигурацию демо без изменения бизнес-логики T-001–T-004, затем добавить два процессных правила в `AGENTS.md`.

Разрешённые файлы ШАГА 0.5: импорты в `backend/*`, `backend/main.py`, `backend/Dockerfile`, `docker-compose.yml`, `.env.example` и необходимые тесты сборки/конфигурации. Разрешённые файлы ШАГА A: `AGENTS.md`. Этот файл попытки также входит в scope.

Ветка: `fix/FIX-001`. Один PR с двумя отдельными коммитами: сначала ШАГ 0.5, затем ШАГ A.

### Просмотренные файлы
- `AGENTS.md`
- `PRODUCT.md`
- `PLAN.md`
- `TASKS.md`
- `docs/TECH_SPEC.md`
- `backend/AGENTS.md`
- `tests/AGENTS.md`
- приложенный пользователем аудит и инструкции
- `docs/agent/tasks/T-001/README_TEMPLATE.md`

### Изменённые файлы
- `AGENTS.md` (отдельный второй коммит, ШАГ A)
- `.env.example`
- `backend/Dockerfile`
- `backend/alembic.ini`
- `backend/main.py`
- `backend/auth/dependencies.py`
- `backend/auth/schemas.py`
- `backend/auth/security.py`
- `backend/db/migrations/env.py`
- `backend/models/group.py`
- `backend/models/queue_entry.py`
- `backend/models/session.py`
- `backend/models/user.py`
- `backend/queue/eta.py`
- `backend/routes/auth.py`
- `backend/routes/queue.py`
- `backend/routes/sessions.py`
- `docker-compose.yml`
- `frontend/src/screens/MyQueues.tsx`
- `tests/auth/conftest.py`
- `tests/auth/test_auth.py`
- `tests/sessions/conftest.py`
- `tests/sessions/test_create_session.py`
- `tests/queue/conftest.py`
- `tests/queue/test_absence.py`
- этот файл попытки

### Команды и результаты
- `docker build -t smart-queue-backend ./backend` -> образ успешно собран из самостоятельного build context.
- отдельный `docker run smart-queue-backend` вне Compose -> миграции применены, Uvicorn запущен.
- `GET /health` отдельного контейнера -> `200`, `{\"status\":\"ok\"}`.
- CORS preflight с `Origin: http://192.168.1.10:5173` -> `200`, origin разрешён из `CORS_ORIGINS`.
- `docker compose up -d --build backend frontend` -> оба сервиса запущены.
- `docker compose run --rm backend pytest /tests/auth /tests/sessions /tests/queue -q` -> `14 passed`.
- `docker compose run --rm frontend npm test` -> `1 test passed`.
- `docker compose run --rm frontend npm run build` -> production-сборка успешна.
- проверка окружения Compose-контейнера -> единственный `PYTHONPATH=/workspace`.
- поиск внутренних импортов без `backend.` и захардкоженного адреса API во frontend -> совпадений нет.

### Блокеры / найденные риски
- Блокеров нет; PR #4 подтверждённо смёржен в `main`.
- Нельзя менять бизнес-логику T-001–T-004.
- Pytest сообщает только внешние предупреждения Starlette/httpx и read-only кэша; тесты проходят.

### Точный следующий шаг
Зафиксировать правила ШАГА A вторым коммитом, показать итоговый diff пользователю и только затем открыть PR. После мержа пользователем перейти к отдельному PR ШАГА B.

### SUBAGENT_HANDOFF
Субагенты не используются.
