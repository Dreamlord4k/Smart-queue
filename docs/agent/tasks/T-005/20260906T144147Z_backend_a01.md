## Задача: T-005
Исполнитель: backend
Начато: 20260906T144147Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Реализовать завершение и пропуск текущей `called`-записи преподавателем-владельцем, обновление `ServiceStat` по EMA с `alpha=0.3`, автоматический вызов следующего студента в освободившийся канал и многоканальный пересчёт ETA.

Разрешённые файлы T-005: `backend/models/service_stat.py`, `backend/models/queue_entry.py`, `backend/routes/queue.py`, `backend/queue/eta.py`, `backend/config.py`, `backend/db/migrations/*`, `tests/queue/*`. По отдельному указанию пользователя в тот же пакет входит точная правка ШАГА C в `PRODUCT.md`.

### Просмотренные файлы
- `AGENTS.md`
- `PRODUCT.md`
- `PLAN.md`
- `TASKS.md`
- этот файл попытки
- `docs/TECH_SPEC.md`
- `backend/AGENTS.md`
- `tests/AGENTS.md`
- текущие модели, queue router, ETA и тесты очереди

### Изменённые файлы
- `PRODUCT.md` — добавлена точная формулировка ШАГА C, остальные пункты MVP перенумерованы;
- `backend/config.py` — единые константы EMA и диапазона ETA;
- `backend/models/service_stat.py` — статистика фактического времени приёма;
- `backend/db/migrations/env.py`, `backend/db/migrations/versions/20260906_0004_service_stats.py` — регистрация модели и миграция;
- `backend/queue/eta.py` — EMA, дисперсия и жадный многоканальный ETA;
- `backend/routes/queue.py` — teacher-only `done`/`skip`, блокировки и автоподъём следующего;
- `tests/queue/conftest.py`, `tests/queue/test_eta.py`, `tests/queue/test_service_flow.py` — очистка новой таблицы и проверки T-005;
- этот файл попытки.

### Команды и результаты
- `python3 -m compileall -q backend tests/queue` -> успешно;
- `docker compose build backend` -> успешно;
- Alembic `upgrade head` на рабочей тестовой БД -> успешно, применена `20260906_0004`;
- Alembic `upgrade head` на отдельной чистой БД -> успешно прошла цепочка `0001`–`0004`; временная БД удалена;
- `pytest -q /tests/queue` -> 13 passed;
- `pytest -q /tests` после финальной пересборки -> 24 passed;
- `npm test -- --run` -> 1 passed;
- `npm run build` -> успешно;
- проверка точной строки ШАГА C и отсутствия хвостовых пробелов -> успешно.

### Блокеры / найденные риски
- Блокеров нет.
- Конкурентный сценарий защищён блокировкой строки сессии и проверен двумя одновременными `done`: один запрос завершился успешно, второй получил 409, канал продвинулся один раз.
- При первой попытке постоянный backend-контейнер не стартовал, потому что локальный порт 8000 уже занят. Сборка успешна; миграции и тесты выполнены одноразовыми контейнерами без публикации порта.
- Pytest сообщил только предупреждения зависимостей Starlette/httpx и невозможность записать cache в read-only mount; на результат тестов они не влияют.

### Точный следующий шаг
Показать итоговый diff, затем через GitHub создать ветку `task/T-005`, закоммитить единый пакет ШАГА C + T-005 и открыть Pull Request без самостоятельного merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
