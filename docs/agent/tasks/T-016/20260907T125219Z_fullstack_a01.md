## Задача: T-016 — демо-панель для комиссии
Исполнитель: fullstack
Начато: 20260907T125219Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Добавить включаемую через `DEMO_MODE` фиксированную панель быстрого входа преподавателем и студентом, стабильный идемпотентный DEMO-UI seed, ручной и 30-минутный сброс, а также независимые demo-сессии в разных вкладках. В demo-режиме не показывать удаление профиля.

Ветка по прямому указанию пользователя: `task/DEMO-panel`. `TASKS.md` не изменяется.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `backend/AGENTS.md`, `tests/AGENTS.md`
- существующие auth, health, frontend session storage, кабинеты, realtime и T-013 demo scripts/tests

### Изменённые файлы
- `backend/demo.py`, `scripts/seed_demo_ui.py` — стабильный идемпотентный DEMO-UI seed: группа, преподаватель, 8 студентов, активная и открытая сессии.
- `backend/routes/auth.py`, `backend/auth/schemas.py`, `backend/auth/security.py` — demo-login, защищённый reset и demo-признак в access/refresh JWT.
- `backend/config.py`, `backend/main.py`, `.env.example`, `docker-compose.yml` — флаг `DEMO_MODE` и его публикация через `/health`.
- `frontend/src/components/DemoPanel.tsx`, `DemoPanel.css`, `frontend/src/App.tsx` — фиксированная панель, две роли, reset и 30-минутный таймер.
- `frontend/src/auth/session.ts` — отдельное `sessionStorage` для demo-токенов каждой вкладки.
- `frontend/src/screens/Settings.tsx` — удаление профиля скрыто для demo-сессии.
- тесты рядом с auth/demo/infra/frontend и `README.md` с запуском.
- этот файл попытки.

### Команды и результаты
- Красный frontend-прогон: 3 test files failed — отсутствовали DemoPanel API/storage и скрытие удаления.
- `tests/demo/test_demo_ui.py` — `4 passed`; seed/login/refresh/reset/запрет удаления.
- `tests/auth + tests/infra + test_demo_ui` — `12 passed`.
- CLI seed запущен повторно успешно, вернул те же UUID двух сессий.
- Полная frontend-регрессия — `62 passed`; production build — успешно.
- Полный backend на локальном SQLite — `57 passed, 5 failed`: существующие тесты с PostgreSQL UUID семантикой (в частности numeric-only UUID SQLite преобразует в integer). Итоговая проверка выполняется Actions на PostgreSQL.
- Smoke UI: при `DEMO_MODE=true` `/health` отдаёт флаг, на странице входа видны обе demo-кнопки.

### Блокеры / найденные риски
- PR #29 смёржен во время работы; ветка T-016 создаётся от нового `main`, его файлы повторно не изменяются.
- Локальная среда не содержит PostgreSQL; обязательна зелёная backend job в Actions.

### Точный следующий шаг
Показать diff ветки `task/DEMO-panel`, открыть PR без merge и дождаться зелёного Actions на PostgreSQL.

### SUBAGENT_HANDOFF
Субагенты не используются.
