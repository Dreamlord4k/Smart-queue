## Задача: T-016 — исправления demo-режима и обязательная причина отказа
Исполнитель: fullstack
Начато: 20260908T141702Z
Предыдущая попытка: `docs/agent/tasks/T-016/20260907T125219Z_fullstack_a01.md`

### Цель и разрешённый scope
Одним пакетом вернуть переключение ролей внутри demo-сессии, сделать общий для вкладок 30-минутный таймер бездействия со сбросом и выходом, потребовать причину отказа на бэкенде и фронте, а также точечно исправить подтверждённые responsive-дефекты на desktop и 375px.

Ветка: `task/DEMO-fix`. Один коммит и один PR по прямому указанию пользователя. `TASKS.md` не изменяется.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, предыдущая попытка T-016
- `backend/AGENTS.md`, `tests/AGENTS.md`, demo/auth/queue API и тесты
- все frontend-экраны обеих ролей и их CSS
- Impeccable: `SKILL.md`, `reference/audit.md`, `reference/craft-floor.md`; контекст загружен для `frontend/src/App.tsx`

### Изменённые файлы
- `frontend/src/demo/activity.ts` и тест — общий для вкладок таймер бездействия на fake time и событие общего reset.
- `frontend/src/components/DemoPanel.tsx`, `DemoPanel.css`, тест — обе роли доступны внутри demo, ручной/автоматический reset выводит на demo-вход с сообщением.
- `frontend/src/App.tsx` и тест — смена demo-токена и кабинета без logout, очистка demo-сессии после reset.
- `backend/routes/queue.py`, `frontend/src/api/student.ts`, `frontend/src/screens/Queue.tsx` и queue-тесты — обязательная непустая причина отказа на обеих сторонах.
- `frontend/src/components/student/Student.css`, `frontend/src/components/teacher/Teacher.css`, `frontend/src/screens/Auth.css`, `frontend/src/theme.css` — точечные исправления 375px, touch-target/focus и переполнений.
- `PRODUCT.md`, `backend/AGENTS.md`, `tests/AGENTS.md` — согласованное правило обязательного `absence_reason`.
- этот файл попытки.

### Команды и результаты
- Impeccable detector (один прогон) нашёл 3 предупреждения об elastic easing; значение заменено на плавное экспоненциальное торможение.
- Узкий frontend-набор: `28 passed`; полный frontend: `76 passed`.
- `npm run build`: успешно.
- Backend `test_absence.py + test_demo_ui.py`: `12 passed`.
- Backend `tests/queue` на SQLite: `31 passed, 1 deselected`; единственный исключённый конкурентный тест использует блокировки/уникальность PostgreSQL и на SQLite воспроизводит прежнюю несовместимость.
- Ручной статический аудит desktop + 375px: auth, обе роли, очереди, настройки, карточки сессий и экран приёма проверены по DOM/CSS; браузерный визуал оставлен человеку по условию задачи.

### Блокеры / найденные риски
- T-016 отсутствует в текущем `TASKS.md`; пользователь явно запретил менять `TASKS.md` и подтвердил использовать T-016 для отчёта.
- Локальный Docker/PostgreSQL не запущен; окончательная проверка конкурентного queue-теста выполняется CI на PostgreSQL.

### Точный следующий шаг
Создать один коммит в `task/DEMO-fix`, показать diff, открыть PR без merge и дождаться зелёного Actions.

### SUBAGENT_HANDOFF
Субагенты не используются.
