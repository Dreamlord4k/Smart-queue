## Задача: T-013 (README: раздел «Обновление на боевом сервере»)
Исполнитель: docs
Начато: 20260907T094326Z
Предыдущая попытка: docs/agent/tasks/T-013/20260907T091515Z_docs_a03.md (Troubleshooting)

### Цель и разрешённый scope
Только `README.md` (новый раздел после запуска, перед тестами — т.е. до
Troubleshooting по файлу) + этот отчёт. Код/тесты/TASKS не тронуты.
Ветка: task/T-013-readme3.

### Содержание раздела
`git pull origin main`; `compose up --build -d` (с https-оверлеем на проде);
проверка `alembic upgrade head` + `curl /health`. Явно: данные/сессии живы
(volume, без `-v`); токены живы при том же `JWT_SECRET`; `.env` не
перезаписывать; фронт после — Ctrl+F5. Домены нейтральные (`<домен>`).

### Изменённые файлы
- README.md (один раздел)
- docs/agent/tasks/T-013/20260907T094326Z_docs_a04.md (этот файл)

### Блокеры
- GitHub-интеграция недоступна: коммит в `task/T-013-readme3` и PR без merge
не выполнены. Требуется человек.

### Точный следующий шаг
- Человеку: закоммитить README.md и отчёт в task/T-013-readme3, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
