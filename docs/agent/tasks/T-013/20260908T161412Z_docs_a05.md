## Задача: T-013 (переработка README под хакатонное MVP)
Исполнитель: docs
Начато: 20260908T161412Z
Предыдущая попытка: docs/agent/tasks/T-013/20260907T094326Z_docs_a04.md (обновление на боевом)

### Цель и scope
Только `README.md` + этот файл. Код, тесты, TASKS.md, архитектура — не тронуты.
Главная мысль: Smart Queue — система управления потоком людей и временем
ожидания, экзамены — первый сценарий, механизм переносим.

### Что и где сделано (по пунктам поручения)
- Начало (`README.md:1–35`): проблема (студент + преподаватель), решение,
«динамический ETA / расчётное время / временное окно» вместо «точный ETA»,
«Для кого» (Студент/Преподаватель). Demo-блока со ссылкой нет — домен
репозиторием не подтверждается.
- Tech Stack (`§Tech Stack`): Backend/Frontend/Infrastructure/Integrations/CI —
каждый пункт сверен (requirements, compose, workflows).
- Architecture: ASCII-схема (Browser→NGINX→Frontend/FastAPI→PG/Redis→WS→Browser)
+ Telegram отдельно; шесть bullets про роль компонентов, без лекций.
- Features: split Для студента / Для преподавателя; добавление опоздавшего
без пересоздания — подтверждено (`add_participant` блокирует только
closed/cancelled).
- Польза для преподавателя, До/после — новое, без обещаний сверх кода.
- ETA: два уровня (пользователь — окно-прогноз; разработчик — формула EMA,
`alpha = 0.3` из `backend/config.py`, capacity>1 из `backend/queue/eta.py`).
- Realtime: цепочка commit→Redis→WS→Frontend, polling 5с как страховка,
пометка «Redis не основная БД».
- Демо: 10-шаговый сценарий защиты + команды синтетики сохранены.
- Quick Start: Requirements/Run/Check/Stop + предупреждение про `down -v`.
- Deployment: сжат, всё полезное сохранено (setup master, HTTPS, webhook,
update, cron); `docs/DEPLOYMENT.md` не создавал — его нет.
- Reliability: Postgres/volume, Redis/backend перезапуск, polling-догон +
честная пометка про отсутствие backup-автоматизации.
- CI: шаги из workflows; названо CI, не CD (автодеплоя нет).
- Beyond Exams: позиционирование + 6 направлений как roadmap, не как готовое.
- Roadmap-инфра: 8 пунктов, без обязательного Kubernetes, цитата про Compose.
- Project Structure: 7 каталогов, сверены с корнем репо.
- Team: Арсений — DevOps/infrastructure (состав задач из поручения).
- Known Limitations: 5 пунктов, каждый сверен с кодом (открытая регистрация
преподавателя, нет refresh rotation, WS-токен в query, токены в
localStorage/sessionStorage, Pub/Sub без durable log).
- Demo Account: синтетика + пароль из кода скрипта.
- Troubleshooting: таблица → подразделы по проблемам, золотое правило intact.
- AI-секция: удалена из README (отдельного дока про AI нет; attempts живы
в `docs/agent/tasks`). License-раздел не добавлял (LICENSE отсутствует).

### Проверки
- Все ссылки на файлы существуют (PRODUCT/TECH_SPEC/PLAN/TASKS/config/eta.py/
setup.sh+ps1/issue+renew/cert-оверлей/оба demo-скрипта/seed_demo_ui/.env.example).
- Команды сверены с compose (сервисы, порты, volumes, CMD с alembic).
- Запрещённые формулировки отсутствуют (grep: точный ETA, revolutionary и др.).
- Код не менялся — тесты/билды не требовались.

### Блокеры
- GitHub-интеграция недоступна: коммит и PR не выполнены. Требуется человек.

### Точный следующий шаг
- Человеку: закоммитить README.md и этот файл, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
