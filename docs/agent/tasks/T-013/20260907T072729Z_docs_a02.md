## Задача: T-013 (README: описание, функции, запуск, тесты, живой показ)
Исполнитель: docs
Начато: 20260907T072729Z
Предыдущая попытка: docs/agent/tasks/T-013/20260907T062125Z_fullstack_a01.md (реализация)

### Цель и разрешённый scope
Только `README.md` + этот отчёт. Код/тесты/TASKS не тронуты. Структура:
описание, функции по TASKS DONE, локальный и глобальный запуск, тесты,
живой показ. Стиль: заголовки, код-блоки, без простыней.

### Сверка фактов (не выдумано)
- TASKS: T-001–T-014 все DONE (проверено grep по «Статус:»).
- Маршруты: auth/register/login/refresh/me, sessions (POST/GET mine/PATCH/participants/freeze),
queue (reorder/lock/done/skip/absence/my-queues/queue-state), telegram (link/init/webhook),
ws `/ws/sessions/{id}`, groups — прочитаны из `backend/routes/*.py`.
- EMA `alpha=0.3`, capacity-каналы, отчёт план/факт — из кода и отчёта T-013.
- Realtime: Redis `session:{id}` + WS + fallback 5с (`pollIntervalMs ?? 5_000`).
- Webhook-секрет: заголовок `X-Telegram-Bot-Api-Secret-Token` (код роута).
- Домен: в репозитории только пример `queue.example.edu`; `queue.emka.lol`
взят из поручения как боевой пример. DuckDNS упомянут как вариант DNS.
- Demo: 2 группы/10 студентов/2 сессии, пароль `Demo-T013-Password!`,
fake-транспорт (29 сообщений, 0 внешних запросов), 5 строк `✓` — из отчёта T-013.
- setWebhook через Bot API: стандартный вызов api.telegram.org (в репо
автоматизации нет — честно показано curl-командой, бот локально в long polling).
- Backend-тесты: папки auth/sessions/groups/queue/notifications/realtime/infra/demo;
«50+» — из отчёта T-013 (53 passed), точное число не фиксировал, чтобы не устареть.

### Изменённые файлы
- README.md (переписан в 4 блока по поручению)
- docs/agent/tasks/T-013/20260907T072729Z_docs_a02.md (этот файл)

### Блокеры
- GitHub-интеграция недоступна: коммит в `task/T-013-readme` и PR без merge
не выполнены. Требуется человек.

### Точный следующий шаг
- Человеку: закоммитить README.md и отчёт в task/T-013-readme, открыть PR без merge.

### SUBAGENT_HANDOFF
Субагенты не используются.
