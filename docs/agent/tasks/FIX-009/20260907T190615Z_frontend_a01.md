## Задача: FIX-009 — убрать дублирующее завершение активной сессии
Исполнитель: frontend
Начато: 20260907T190615Z
Предыдущая попытка: нет

### Цель и разрешённый scope
Не показывать преподавателю рядом два действия, завершающих активную или приостановленную сессию: явная отмена доступна только до начала приёма, после начала остаётся штатное закрытие с отчётом.

Ветка: `fix/FIX-009`. `TASKS.md` не изменяется.

### Просмотренные файлы
- `AGENTS.md`, `PRODUCT.md`, `PLAN.md`, `TASKS.md`, `docs/TECH_SPEC.md`
- `frontend/src/screens/TeacherSession.tsx` и тест

### Изменённые файлы
- `frontend/src/screens/TeacherSession.tsx` и тест
- этот файл попытки

### Команды и результаты
- `TeacherSession.test.tsx`: 7 passed.
- Полный frontend: 15 файлов, 73 теста — passed.
- Production build — passed.

### Блокеры / найденные риски
- Нет.

### Точный следующий шаг
Создать один коммит в `fix/FIX-009`, показать diff, открыть PR без merge и дождаться CI.

### SUBAGENT_HANDOFF
Субагенты не используются.
